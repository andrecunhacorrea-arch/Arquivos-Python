#exemplo de Tupla
coordenadas = (3,7) #aqui eu criei uma tupla de numeros interiros (INT).
print(coordenadas[0])
print(coordenadas[1])

#A tupla é imutável, ou seja, não podemos alterar os valores dela.
#coordenadas[0] = 10 #isso vai gerar um erro, pois não podemos alterar os valores de uma tupla.
